<table border="2px" align="center" width="30%", style="background-color:violet "><td><p id="notice"><%= notice %></p>

<h1>Categories</h1>

<table>
  <thead>
    <tr>
      <th>Name</th>
      <th>Desc</th>
      <th>Image</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @categories.each do |category| %>
      <tr>
        <td><%= category.name %></td>
        <td><%= category.desc %></td>
        <td><%= image_tag category.image_url.to_s, style:"height: 70px; width: 100px;" %></td>
        <td><%= link_to 'Show', category,class:"btn btn-success"%></td>
        
        <td><%= link_to 'Edit', edit_category_path(category),class:"btn btn-primary" %></td>
        <td><%= link_to 'Destroy', category, method: :delete, data: { confirm: 'Are you sure?' } ,class:"btn btn-danger"%></td>
        
      </tr>
    <% end %>
  </tbody>
</table>

<br>
<%= link_to 'Back', products_path, class:"btn btn-success" %>
<%= link_to 'New Category', new_category_path, class:"btn btn-success" %>
</td>
</table>